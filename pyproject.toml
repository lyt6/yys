[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--strict-config --strict-markers"

[tool.ruff]
target-version = "py310"
line-length = 100
exclude = ["account_pool.py", "browser_profiles", "data"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
